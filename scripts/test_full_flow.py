# ============================================================
# 文件：scripts/test_full_flow.py
# 说明：E2E 测试入口脚本
# ============================================================

"""
E2E 完整测试流程入口

这是 E2E 测试的主入口脚本，执行以下操作：
1. 将项目根目录添加到 Python 路径
2. 导入并执行 full_flow.main()

使用方法:
    python scripts/test_full_flow.py

或者从项目根目录运行：
    python -m scripts.test_full_flow
"""

#!/usr/bin/env python3

import sys
from pathlib import Path


# ============================================================
# 路径配置
# ============================================================
# 获取项目根目录：当前文件的上上级目录
ROOT_DIR = Path(__file__).resolve().parent.parent

# 将根目录添加到 Python 路径（支持导入 scripts 模块）
if str(ROOT_DIR) not in sys.path:
    sys.path.insert(0, str(ROOT_DIR))

# 导入主测试流程函数
from scripts.tests.full_flow import main


# ============================================================
# 程序入口
# ============================================================
if __name__ == "__main__":
    # 执行 E2E 测试
    main()


"""
============================================================
知识点讲解 (Python 入口脚本)
============================================================

1. shebang 行
   #!/usr/bin/env python3

   - 在 Unix/Linux/Mac 系统上指定解释器
   - env 命令在 PATH 中查找 python3
   - Windows 忽略此行

2. sys.path 操作
   sys.path.insert(0, str(ROOT_DIR))

   - 将项目根目录添加到导入搜索路径
   - 位置 0 表示最高优先级
   - 支持从 scripts 目录导入模块

3. __name__ == "__main__"
   - Python 的入口点约定
   - 直接运行时：__name__ 为"__main__"
   - 被导入时：__name__ 为模块名

============================================================
目录结构
============================================================

project_root/
├── scripts/
│   ├── test_full_flow.py    # 本文件（入口）
│   └── tests/
│       ├── config.py        # 配置
│       ├── specs.py         # 数据规格
│       ├── fixtures.py      # 夹具构建
│       ├── session.py       # 会话管理
│       ├── validators.py    # 验证函数
│       └── full_flow.py     # 完整流程
└── ...

============================================================
运行方式
============================================================

方式 1: 直接运行
    python scripts/test_full_flow.py

方式 2: 使用-m 参数
    python -m scripts.test_full_flow

方式 3: 在 scripts 目录下
    cd scripts
    python test_full_flow.py

============================================================
测试前置条件
============================================================

运行此脚本前需要：

1. Docker 已安装并运行
   - 用于启动 PostgreSQL 容器

2. Rust/Cargo 已安装
   - 用于编译和运行 API 服务

3. 样题 ZIP 文件存在
   - scripts/test.zip（6 道理论题）
   - scripts/test2.zip（4 道实验题）

4. 端口可用
   - 55433: PostgreSQL
   - 18080: API 服务

============================================================
测试流程概览
============================================================

[1/9] 构建夹具 ZIP
  ├─ 3 道合成题目
  ├─ 2 个试卷附录
  ├─ 6 道真实理论题（test.zip）
  └─ 4 道真实实验题（test2.zip）

[2/9] 启动 PostgreSQL 容器
  └─ Docker run postgres:14.1

[3/9] 应用数据库迁移
  └─ 执行 migrations/0001_init_pg.sql

[4/9] 启动 API 服务
  └─ cargo run

[5/9] 合成题目测试
  ├─ CRUD 操作
  ├─ 查询参数
  └─ Bundle 下载

[6/9] 真实理论题测试
  ├─ 上传 6 道题目
  ├─ 创建 2 份试卷
  └─ Bundle 下载验证

[7/9] 真实实验题测试
  ├─ 上传 4 道题目
  ├─ 创建 2 份试卷
  └─ Bundle 下载验证

[8/9] Ops API 和清理
  ├─ 导出 JSONL
  ├─ 质量检查
  └─ 删除所有创建的数据

[9/9] 生成 Markdown 报告
  └─ tmp/qb_e2e_report.md

============================================================
输出文件
============================================================

测试完成后在 tmp/ 目录生成：

| 文件                      | 说明                    |
|---------------------------|-------------------------|
| qb_api_e2e.log           | API 运行日志           |
| qb_e2e_internal.jsonl    | 导出的题目数据         |
| qb_e2e_quality.json      | 质量检查报告           |
| qb_e2e_report.md         | 完整测试报告（Markdown）|
| samples/                 | 测试用 ZIP 文件        |
| downloads/               | 下载的 Bundle ZIP      |

============================================================
故障排查
============================================================

1. Docker 容器启动失败
   - 检查 Docker 是否运行
   - 检查端口 55433 是否被占用

2. API 启动失败
   - 检查 Rust 编译是否通过
   - 检查端口 18080 是否被占用

3. 找不到 test.zip/test2.zip
   - 确认文件在 scripts/目录下
   - 检查文件权限

4. 测试中途失败
   - 查看 tmp/qb_e2e_report.md
   - 查看 tmp/qb_api_e2e.log
   - 清理残留容器：docker rm -f qb-postgres-e2e
"""
